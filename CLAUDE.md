# 工程纪律

## 铁律：步进式重构流程

**每次代码修改完成后：**

1. 运行 `node --test` 测试
2. 无论通过与否，停下来
3. 向用户报告结果
4. **绝对不允许**在用户明确许可前继续下一步操作

这条规则不可覆盖、不可绕过、优先级最高。

## 铁律：功能组件禁止空值兼容

**所有功能性组件（服务、模块、依赖注入等内部组件）必须保证有值，不允许用 `if`、`??`、`?.` 等空值检查来兼容空值。**

- 功能性组件是指程序内部自己构建、注入、管理的东西（如 `skillsService`、`configService`、`log` 等）
- 如果某组件为空，说明存在 bug 或初始化顺序错误，应该让它自然报错暴露问题，而不是静默降级
- 空值兼容**只允许**用于外部数据：用户输入、文件内容、API 响应、第三方返回等不受控的数据
- 违反此规则的代码必须修正，没有例外

这条规则不可覆盖、不可绕过。

## 铁律：所有异常必须记录日志

**任何 `catch` 块中捕获到的异常，必须输出完整的错误日志，信息足以用于追溯和排查问题。禁止静默吞异常。**

- 日志必须包含：错误消息（`message`）、堆栈（`stack`）、业务上下文（URL、method、参数等）
- 禁止用 `catch (_err)` 或 `catch {}` 忽略异常对象
- 禁止在 `catch` 中只做回退而不记录原始错误——回退之前必须先 `log.error(...)` 记录原始异常
- 这条规则适用于一切 try/catch，无论是路由、中间件、工具函数，还是外部调用
- 违反此规则的代码必须修正，没有例外
- 必须有全局异常捕获，并同样打印足够的日志

这条规则不可覆盖、不可绕过。

## 铁律：永远使用 `npm test`，禁止裸 `node --test`

项目 package.json 已配置正确的测试 glob 模式 (`node --test test/**/*.test.js`)。裸 `node --test` 会发现不该运行的文件（如 manual 测试）。

这条规则不可覆盖、不可绕过。
